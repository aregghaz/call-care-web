type TypeDatabase = {
    db: string,
    services: string,
    global: string
}

const databaseInfo:TypeDatabase =  {
    db: "/db.json",
    services: "services",
    global: "global",
}

export default databaseInfo