type TypeDatabase = {
    db: string,
    services: string,
    global: string
}

const databaseInfo =  {
    db: "http://localhost:4200",
    services: "services",
    global: "global",
}

export default databaseInfo