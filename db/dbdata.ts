type TypeDatabase = {
    db: string,
    services: string,
    global: string,
    work: string,
}

const databaseInfo:TypeDatabase =  {
    db: "/db.json",
    services: "services",
    global: "global",
    work: "work",
}

export default databaseInfo